# 大学生全栈项目实训平台需求文档

**版本：** V1.0  
**范围：** 微信小程序展示端 + Web 管理后台  
**依据：** 当前《项目实战小程序交互原型.html》和《项目实战管理后台原型.html》  
**定位：** 由个人维护的项目作品展示平台，访客浏览项目、了解技术与功能；管理员维护展示内容。

## 1. 产品目标与角色

### 1.1 产品目标

1. 集中展示项目的简介、主要技术、图片、视频及具体功能。
2. 让访客通过搜索和技术分类快速找到感兴趣的项目。
3. 让管理员自行更新项目、素材、个人介绍和联系方式。
4. 由管理员控制是否公开联系方式；关闭时，展示端不出现任何联系方式和联系引导。

### 1.2 用户角色

| 角色 | 使用入口 | 权限 |
| --- | --- | --- |
| 访客 | 微信小程序 | 查看已发布项目、搜索筛选、浏览项目与功能详情、查看“关于我”；联系方式开启时可查看“联系我” |
| 管理员 | Web 管理后台 | 登录后台，新增、编辑、发布、下架项目，上传素材，编辑“关于我”和联系方式，切换联系方式展示状态 |

首版仅设一名管理员，无访客登录、注册、评论、收藏、课程购买或支付功能。

## 2. 展示端信息架构

```text
项目页
├─ 项目搜索
├─ 技术分类筛选
├─ 项目列表
└─ 项目详情
   ├─ 项目展示图片
   ├─ 演示视频
   ├─ 项目简介
   ├─ 主要技术
   └─ 核心功能列表
      └─ 单项功能详情
         ├─ 具体功能描述
         ├─ 详细截图
         └─ 功能流程
关于我
联系我（由后台开关控制）
```

页面采用清新浅色风格，手机端优先。项目页直接从搜索框、技术分类和项目列表开始，**不展示顶部宣传介绍卡片**。

## 3. 展示端功能需求

### F-01 项目列表、搜索和筛选

- 默认只展示状态为“已发布”的项目，按后台设置的排序值升序展示；排序值相同时按更新时间倒序。
- 每张项目卡片展示封面图、名称、主要技术标签、难度、简短简介；点击卡片进入项目详情。
- 支持按项目名称、主要技术进行关键词搜索；支持按技术方向筛选。搜索词与分类条件同时生效。
- 搜索结果展示项目数量；无结果时展示空状态和清空搜索入口。
- 项目列表顶部不设置“探索项目”等宣传按钮。

### F-02 项目详情

- 展示项目名称、技术方向、难度、项目简介和主要技术。
- 展示项目图片轮播/缩略图；点击缩略图可切换大图，点击大图可全屏查看。
- 有演示视频时展示播放器及封面；没有视频时隐藏视频区块，不显示“上传视频”之类的管理操作。
- 展示核心功能列表；点击某项功能进入该功能的二级详情。
- 不显示“联系我了解这个项目”按钮。

### F-03 单项功能详情

- 展示功能名称、具体功能描述、功能截图和操作流程。
- 支持多张截图，按后台排序展示；点击截图可放大查看。
- 有明确的“返回项目详情”入口，返回后保持项目详情状态。
- 不显示“联系我了解实现细节”按钮。

### F-04 关于我

- 展示管理员维护的页面标题、个人介绍和擅长方向。
- 不显示“查看项目”按钮。
- 联系方式关闭时，不显示任何指向“联系我”页面的文字或入口。

### F-05 联系方式展示开关

- 后台设置 `show_contact`，默认开启；管理员可随时修改。
- **开启：** 底部导航显示“联系我”，联系页展示标题、说明和微信号，可复制微信号。
- **关闭：** 底部导航隐藏“联系我”；联系页不可通过链接或直接访问进入；“关于我”等其他页面隐藏联系引导、微信号、二维码及任何联系方式相关信息。
- 关闭时，客户端不能仅靠隐藏按钮：服务端公开接口也不返回微信号等联系内容。
- 开关变更后，展示端重新获取配置时生效；已打开的页面在返回前台或刷新时同步最新状态。

## 4. 管理后台功能需求

### A-01 管理员登录

- 后台必须登录后使用；首版仅一名管理员。
- 登录状态过期后返回登录页；未登录者不能访问管理接口和上传接口。
- 密码不在前端代码中明文保存。

### A-02 概览

- 显示项目总数、已发布数量、草稿数量、素材数量。
- 显示最近更新的项目及其状态。
- 提供“新建项目”“项目管理”“联系方式”快捷入口。

### A-03 项目管理

- 项目列表支持按名称或技术搜索、按“全部/已发布/草稿”筛选。
- 可新建、编辑、保存草稿、发布、下架、删除项目。
- 删除操作需二次确认；正式系统建议采用软删除，避免误删后无法恢复。
- 发布前校验必填字段：项目名称、技术方向、项目简介、至少一项主要技术、封面图、至少一项核心功能。
- 列表页显示名称、技术方向、功能数量、状态、更新时间和操作入口。

### A-04 项目基本信息

| 字段 | 要求 | 展示位置 |
| --- | --- | --- |
| 项目名称 | 必填，建议不超过 40 字 | 列表与详情标题 |
| 技术方向 | 必填，单选：Java、Python、小程序、AI 应用、其他；后台可扩展 | 筛选项与卡片 |
| 难度 | 可选：入门、进阶、高级 | 项目卡片与详情 |
| 主要技术 | 必填，可添加多个技术标签 | 项目卡片与详情 |
| 简短简介 | 必填，建议不超过 100 字 | 项目卡片 |
| 项目简介 | 必填，支持多段文字 | 项目详情 |
| 排序值 | 可选，数字 | 列表排序 |
| 发布状态 | 草稿/已发布 | 控制前台可见性 |

如果首版希望减少录入工作，可先用“项目简介”的前 100 字自动生成简短简介，但后台应允许单独修改。

### A-05 项目图片与视频

- 上传项目封面图 1 张、展示图片多张、演示视频 0～1 个。
- 图片上传后可预览、删除、调整顺序；每张图可填写简短说明。
- 视频上传后可预览、替换和删除；视频应有封面图或自动生成封面。
- 文件上传必须真正保存到服务端或对象存储，后台保存的是文件地址和元数据，而不是本地文件名。
- 具体格式、大小上限和存储方案由开发实施时确定，并在上传控件旁明确提示。

### A-06 核心功能管理

- 每个项目可增加、编辑、删除、排序多项核心功能。
- 每项功能维护：功能名称、具体功能描述、操作流程、详细截图多张及截图顺序。
- 后台预览应与展示端二级详情结构一致。
- 功能名称和描述为必填；没有真实截图的项目可保存草稿，但发布前应提示缺少截图。

### A-07 关于我与联系方式

- “关于我”维护页面标题、个人介绍和擅长方向。
- “联系方式”维护联系页标题、说明、微信号，以及“在前台展示联系方式”开关。
- 保存开关时显示当前状态；关闭后字段仍保留在后台，以便重新开启时恢复。
- 后台提供“预览前台”入口，检查开关开启和关闭后的展示效果。

## 5. 数据与接口建议

### 5.1 核心数据

| 实体 | 主要字段 |
| --- | --- |
| Project | id、name、category、level、summary、description、status、sort_order、created_at、updated_at |
| ProjectTech | project_id、tech_name、sort_order |
| ProjectMedia | id、project_id、type（cover/image/video）、url、caption、sort_order |
| ProjectFeature | id、project_id、name、description、steps、sort_order |
| FeatureImage | id、feature_id、url、caption、sort_order |
| SiteProfile | title、introduction、skills |
| SiteContact | show_contact、title、description、wechat_id |
| AdminUser | id、account、password_hash、status |

### 5.2 公开接口

| 接口用途 | 必须遵守的规则 |
| --- | --- |
| 获取项目列表 | 只返回已发布项目；支持关键词、分类和分页 |
| 获取项目详情 | 草稿及已删除项目不可公开访问 |
| 获取功能详情 | 仅在所属项目已发布时可访问 |
| 获取站点配置 | 返回公开的“关于我”和 `show_contact`；当 `show_contact=false` 时，不返回微信号及联系页内容 |

管理接口需覆盖项目、素材、功能、个人资料和联系设置的增删改查；全部要求管理员登录。上传接口应校验文件类型、大小和登录身份。

## 6. 关键业务规则

1. 草稿不出现在小程序列表、搜索结果或直接访问的详情页。
2. 下架项目立即从公开接口移除，后台内容保留。
3. 删除项目时连同其功能和素材关系处理；删除存储文件应有明确的清理策略。
4. 联系方式总开关优先级最高：关闭时无论前台页面、缓存或接口，都不得公开联系内容。
5. 搜索和分类筛选基于同一份已发布项目数据，切换条件后数量同步更新。
6. 图片和视频缺失时，对应展示区块隐藏或显示合理空状态，不显示后台上传控件。

## 7. 验收标准

| 场景 | 预期结果 |
| --- | --- |
| 管理员保存草稿 | 后台可继续编辑；小程序不可搜索或访问该项目 |
| 管理员发布完整项目 | 项目出现在列表与搜索结果；详情包含图片、简介、技术和核心功能 |
| 访客点击核心功能 | 进入二级详情，看到具体描述、截图和流程，可返回项目详情 |
| 访客输入关键词并选择分类 | 结果同时满足两项条件，数量正确；无结果时有空状态 |
| 管理员关闭联系方式 | 底部“联系我”、联系页及所有联系引导消失；公开接口不返回微信号 |
| 管理员重新开启联系方式 | 联系入口和已保存的内容恢复展示 |
| 管理员上传素材 | 刷新后台后仍可看到文件，前台能正常加载和播放 |
| 未登录用户访问后台接口 | 请求被拒绝，不能获取或修改后台数据 |

## 8. 原型与正式产品的差异

当前两个 HTML 文件仅用于确认页面与交互。展示端使用示例项目与示意截图；管理后台的文字保存在浏览器本地，图片和视频仅记录文件名；两个本地文件之间的“预览前台”通过预览参数演示联系方式开关。**这些都不等于真实上传、跨设备同步或正式发布。**

正式开发需补齐：数据库、后台登录、文件存储、公开与管理 API、发布状态校验、联系方式服务端开关，以及微信小程序端对接口的接入。

## 9. 首版范围外

首版暂不包含：学员账号、报名、课程销售、在线支付、评论、收藏、在线客服、消息推送、项目源码下载和多管理员权限体系。如以后需要，可另开版本设计。
